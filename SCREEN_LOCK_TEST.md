# 螢幕鎖定功能測試流程

## 前置準備

### 1. 啟用無障礙服務

**為什麼需要？**
- 無障礙服務讓 App 可以監控其他 App 的使用狀態
- 可以偵測使用者是否切換到其他 App，並在需要時把 App 帶回前台

**啟用步驟：**

1. 在 App 內開啟開發者模式（參考 [DEVELOPER_MODE.md](DEVELOPER_MODE.md)）
   - 快速點「你好！」文字 2 次
   - 快速點「數位健康提醒」卡片 2 次

2. 在測試控制區會看到無障礙服務狀態
   - 如果顯示「❌ 未啟用」，點擊「開啟無障礙設定」

3. 在 Android 系統設定中：
   - 找到「Kids Time Control」或「Lock Accessibility Service」
   - 打開開關
   - 系統會警告「這個 App 可以監控你的操作」→ 點「確定」或「允許」

4. 返回 App，確認顯示「✅ 已啟用」

### 2. 確認 Android 版本

- Screen Pinning API 需要 **Android 5.0 (Lollipop)** 以上
- 查看方式：設定 > 關於手機 > Android 版本

---

## 測試流程

### 步驟 1：啟動鎖定

1. 在 App 內開啟開發者模式
2. 找到「測試控制」區塊
3. 點擊「**鎖定裝置**」按鈕
4. 觀察是否有變化

### 步驟 2：測試上滑手勢

1. 從螢幕底部**往上滑**
2. **預期結果**：
   - ✅ **成功**：無法離開 App，停留在鎖定畫面
   - ❌ **失敗**：可以看到主畫面或最近使用的 App

### 步驟 3：測試切換 App

1. 按**多工鍵**（最近使用的 App 按鈕）
2. 或從螢幕底部往上滑並停留（手勢導航）
3. **預期結果**：
   - ✅ **成功**：無法開啟多工選單
   - ❌ **失敗**：可以看到多工選單並切換 App

### 步驟 4：測試返回鍵

1. 按**返回鍵**
2. **預期結果**：
   - ✅ **成功**：沒有反應，繼續停留在鎖定畫面
   - ❌ **失敗**：App 關閉或返回上一頁

### 步驟 5：測試通知列

1. 從螢幕頂部**往下滑**
2. **預期結果**：
   - ✅ **成功**：無法拉下通知列
   - ❌ **失敗**：可以拉下通知列並點擊通知

### 步驟 6：解除鎖定

有三種方式可以解除鎖定：

**方式 1：家長 PIN 碼解鎖（正式流程）**
1. 在鎖定畫面，**點擊大鎖頭圖示 5 次**
2. 輸入家長 PIN 碼（預設：`1234`）
3. 選擇增加時間（+5 分鐘或 +10 分鐘）
4. 鎖定自動解除

**方式 2：開發者模式解鎖（測試用）**
1. 如果開發者模式還在，滑動找到測試控制區
2. 點擊「**解鎖裝置**」按鈕

**方式 3：Screen Pinning 系統解鎖**
1. 同時按「返回鍵 + 多工鍵」
2. 這會解除 Screen Pinning（如果有啟用的話）
3. 但 App 的鎖定狀態仍然有效

---

## 測試結果記錄

### 方案 1: Screen Pinning API

| 測試項目 | 結果 | 說明 |
|---------|------|------|
| 上滑離開 | ⬜ 通過 / ⬜ 失敗 | |
| 切換 App | ⬜ 通過 / ⬜ 失敗 | |
| 返回鍵 | ⬜ 通過 / ⬜ 失敗 | |
| 通知列 | ⬜ 通過 / ⬜ 失敗 | |
| 整體評價 | ⬜ 可用 / ⬜ 不可用 | |

**錯誤訊息（如果有）：**
```
請在此貼上 Logcat 中的錯誤訊息
```

---

## 常見問題

### Q: 點「鎖定裝置」後沒反應？

**可能原因：**
1. Screen Pinning 沒有權限（會有 Exception）
2. 需要在 Logcat 中查看錯誤訊息

**解決方式：**
- 查看電腦上的 Logcat 輸出（在 `flutter run` 的終端中）
- 找到 `startLockTask` 相關的錯誤

### Q: 無障礙服務無法啟用？

**解決方式：**
1. 確認 App 有無障礙權限
2. 重新安裝 App
3. 重啟手機

### Q: Screen Pinning 被使用者解除？

**說明：**
- Screen Pinning 允許使用者透過特定手勢解除（返回鍵 + 多工鍵）
- 這是方案 1 的限制
- 如果不可接受，需要改用方案 2 或 3

---

## 下一步

- ✅ **如果方案 1 成功**：繼續優化使用者體驗
- ❌ **如果方案 1 失敗**：測試方案 2（Accessibility Service 強化）

---

**測試日期**：______
**測試裝置**：______
**Android 版本**：______
**測試結果**：______
